-- i.e. allow new exclude fields, ...

-- we just add new fields and leave alone what was before
-- update be_groups set non_exclude_fields=CONCAT(non_exclude_fields, ',', ',tt_content:frame_class,tt_content:uploads_description,tt_content:uploads_type') where title='priv-Content';

